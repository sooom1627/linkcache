import { view } from "./storybook.requires";

const StorybookUI = view.getStorybookUI({
  // initialSelection: "design-system-colors--palette",
  shouldPersistSelection: true,
});

export default StorybookUI;
